<section class="plan <%= popular(plan) %>" data-sku="<%= plan.sku %>">
  <h3><%= plan.name %></h3>
  <%= render grid_partial_for(plan) %>
  <div class="price"><%= plan_price(plan) %></div>
  <%= choose_plan_link(plan) %>
</section>
