<li class="note mentor" data-role="note">
  <span class="time"><%= note.created_at.strftime('%B %d') %></span>
  <%= image_tag gravatar_url(current_user, :size => '50'), class: 'mentor-image' %>
  <%= note.body_html.html_safe %>
</li>

