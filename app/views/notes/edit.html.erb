<h3>Edit the note</h3>
<%= form_for @note, url: note_path(@note, timeline_user_id: params[:timeline_user_id]), html: { class: 'note-form edit' } do |f| %>
  <fieldset>
    <%= f.text_area :body %>
    <%= f.submit 'Save changes' %>
  </fieldset>
<% end %>
