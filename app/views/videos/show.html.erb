<%= render 'learn_header', subject: @purchase.purchaseable_name %>

<section id="promo-wrapper">
  <h3 class="video-headline">Watch or Download Video</h3>
  <%= render @video %>
</section>

<%= render 'videos/footer' %>
