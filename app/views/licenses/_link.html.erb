<% if current_user_has_active_subscription? %>
  <div class="license">
    <% if included_in_current_users_plan?(offering) %>
      <%= render "licenses/included", offering: offering %>
    <% else %>
      <%= render "licenses/not_included", offering: offering %>
    <% end %>
  </div>
<% end %>
