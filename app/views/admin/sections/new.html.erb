<%= semantic_form_for [:admin, @workshop, @section] do |form| %>
  <%= @section.errors.full_messages.join(', ') %>
  <%= render partial: 'inputs',
             locals: { form: form,
                           workshop: @workshop,
                           teachers: @teachers } %>

  <%= form.actions do %>
    <%= form.action :submit, label: 'Save Section' %>
  <% end %>
<% end %>
