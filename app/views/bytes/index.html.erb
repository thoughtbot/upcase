<% content_for :page_title, t('shared.bytes') %>
<% content_for :subject_block do %>
  <h1><%= t('shared.bytes') %></h1>
<% end %>

<div class="text-box-wrapper solo">
  <div class="text-box">
    <% if @bytes.present? %>
      <section class="bytes results">
        <%= render @bytes %>
      </section>
    <% end %>
  </div>
</div>
