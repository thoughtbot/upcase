<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>thoughtbot training</title>
    <%= stylesheet_link_tag 'reset', 'defaults', 'type', 'forms', 'tables', 'lists', 'screen' %>
  </head>
  <body class="<%= body_class %>">
    <div class="top">
      <div class="wrapper">
        <div id="header">
          <%= render :partial => 'shared/flashes' -%>
          <h1 id="logo"><a href="/">thoughtbot training</a></h1>
          <ul id="nav">
            <% [link_to("Admin",    "#"),
             link_to("Sign out", "#")].each do |link| %>
              <li><%= link %></li>
            <% end %>
          </div>
        </div>
      </div>
      <%= yield %>
      <%= render :partial => 'shared/javascript' %>
    </div>
  </body>
</html>
