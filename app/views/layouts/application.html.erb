<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag 'application' %>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <% if content_for? :description -%>
      <meta name="description" content="<%= yield(:description) %>" />
    <% else -%>
      <meta http-equiv="Description" name="Description" content="<%= t(".meta-description", default: "Workshops to help you get a job as a designer or developer at a web startup.") %>" />
    <% end -%>
    <% if content_for? :keywords -%>
      <meta name="keywords" content="<%= yield(:keywords) %>" />
    <% else -%>
      <meta http-equiv="Keywords" name="Keywords" content="<%= t(".meta-keywords", default: "web designer job, web design job, web development job, startup designer, startup developer, web startup job, startup job") %>" />
    <% end -%>
    <script type="text/javascript" src="https://use.typekit.com/jes4mbj.js"></script>
    <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
    <%= csrf_meta_tag %>
    <!--[if lt IE 9]>
    <script src="//html5shiv.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <script type="text/javascript" src="https://js.stripe.com/v1/"></script>
  </head>

  <body class="<%= body_class %>">
    <%= render 'shared/header' %>

    <%= render 'shared/flashes' %>

    <%= yield %>

    <%= render 'shared/footer' %>

    <%= render partial: 'shared/javascript' %>
  </body>
</html>
