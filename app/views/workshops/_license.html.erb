<section id="license" class="<%= workshop.offering_type %>">
  <%= render 'offerings/prime_purchase', offering: workshop %>

  <% if sections.any? %>
    <%= render 'sections/section', sections: sections, workshop: workshop %>
  <% else %>
    <%= render 'sections/tbd' %>
  <% end %>
</section>
