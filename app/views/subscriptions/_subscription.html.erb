Active since: <%= subscription.created_at.strftime("%D") %> <%= link_to t('subscriptions.cancel'), subscription, method: :delete, confirm: 'Are you sure you want to cancel your Prime subscription?' %>
