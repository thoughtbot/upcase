<% content_for :page_title, "Upcase" %>
<% content_for :additional_body_classes, "landing" %>

<%= render 'subscriptions/hero', community_size: @landing_page.community_size %>

<%= render 'subscriptions/topics_grid', topics: @landing_page.topics %>

<%= render 'subscriptions/trails', example: @landing_page.example_trail %>

<%= render 'subscriptions/support' %>

<%= render 'subscriptions/price',
  primary: @landing_page.primary_plan,
  secondary: @landing_page.secondary_plan %>

<%= render 'subscriptions/testimonials' %>
