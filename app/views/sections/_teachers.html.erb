<ul class="teachers">
  <% teachers.each do |teacher| %>
    <%= content_tag(:li, teacher) do %>
      <%= teacher_gravatar(teacher, 92) %>
      <h2><span>Taught by</span> <%= teacher.name %></h2>
      <section class="bio">
        <%= raw textilize teacher.bio %>
      </section>
      <p class="expand-bio"><a href="#">more...</a></p>
    <% end %>
  <% end %>
</ul>
