<% if !current_user_has_active_subscription? %>
  <section class="subscription">
    <%= link_to "Subscribe to #{t('shared.subscription.name')} to access all of these products", subscribe_path, class: 'button' %>
  </section>
<% end %>

<h2>Every workshop</h2>
<section class="resources">
  <%= render(
    partial: "products/workshops/workshop",
    collection: @catalog.workshops
  ) %>
</section>

<h2>Every show</h2>
<section class="resources">
  <%= render(
    partial: "products/shows/show",
    collection: @catalog.shows
  ) %>
</section>

<h2>Every screencast</h2>
<section class="resources">
  <%= render(
    partial: "products/screencasts/screencast",
    collection: @catalog.screencasts
  ) %>
</section>

<h2>Every exercise</h2>
<section class="resources">
  <%= render(
    partial: "products/exercises/exercise",
    collection: @catalog.exercises
  ) %>
</section>

<h2>Every book</h2>
<section class="resources">
  <%= render(
    partial: "products/books/book",
    collection: @catalog.books
  ) %>
</section>
