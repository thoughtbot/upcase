<h1 class="topic-label"><%= @quiz.title %></h1>

<div class="flashcard">
  <span class="flashcards-count">
    <%= t(".numbered-title",
          position: @flashcard.position,
          total: @quiz.length) %>
  </span>

  <div class="flashcard-prompt">
    <%= format_markdown @flashcard.prompt %>
  </div>
</div>

<% if @reviewing %>
  <%= format_markdown @flashcard.answer %>
  <%= link_to t(".return-to-results"), quiz_results_path(@flashcard.quiz) %>
<% else %>
  <h4><%= t(".answer") %></h4>
  <%= render "flashcards/hidden_answer",  flashcard: @flashcard %>
<% end %>
