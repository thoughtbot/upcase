<% content_for :additional_content_classes, "content-narrow" %>

<% content_for :head do %>
  <%= auto_discovery_link_tag(:rss, show_url(@show, format: "rss")) %>
<% end %>

<%= render "products/meta", product: @show %>

<span class="divider">
  <h4 class="text">Videos</h4>
</span>

<div class="text-box">
  <div class="video-and-assets-wrapper">
    <%= render Clip.new("ol6e0miehm") %>
  </div>

  <section id="videos">
    <%= render @show.videos_with_status %>
  </section>
</div>
