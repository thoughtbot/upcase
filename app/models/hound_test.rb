class HoundTest
  def method
    this && is && a && really && long && line && and && I && hope && that && hound && will && complain && about && it && because && lets && be && honest && thats && its && job && you && know
  end
end
